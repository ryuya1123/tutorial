fn main() {
    let A, B = 0.0;
    let i, j, k;
    let z[1760];
    let b[1760];
    println!("\x1b[2J");
    loop {
        memset(b, 32, 1760);
        memset(z,  0,  7040);
        for j in (0..6.28) {
            for i in (0..6.28) {
                let c = 
                i += 0.02;
            }
            j += 0.07;
        }
    }
}
